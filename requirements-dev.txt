# Extra dependencies for the LOCAL data-refresh scripts only (refresh_ubon_data.py,
# backfill_ubon_weekly.py, geo_boundary.fetch_roads, export_ubon_monthly_gee.py,
# import_shapefiles.py). Not needed by the deployed dashboard itself - keeps
# Streamlit Cloud's install lean.
-r requirements.txt
earthengine-api>=1.4
scipy>=1.11
seaborn>=0.13
# Reads the Thai .shp/.dbf files for import_shapefiles.py. Pure Python, no GDAL
# build needed; the reprojection it feeds uses rasterio, already a dependency.
pyshp>=2.3
# shapely.coverage_simplify, added in 2.1, is what keeps a border shared
# between two districts from being thinned into two different lines - see
# import_shapefiles.thin_all(). Raised here rather than in requirements.txt
# because only the local build needs it: the deployed app reads the GeoJSON
# this produces and never simplifies anything, so its install stays lean.
shapely>=2.1
