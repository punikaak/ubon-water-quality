# Extra dependencies for the LOCAL data-refresh scripts only (refresh_ubon_data.py,
# backfill_ubon_weekly.py, geo_boundary.fetch_*, export_ubon_monthly_gee.py). Not
# needed by the deployed dashboard itself - keeps Streamlit Cloud's install lean.
-r requirements.txt
earthengine-api>=1.4
scipy>=1.11
seaborn>=0.13
